# WeatherApp 🌤️

Простое веб-приложение для отображения погоды с использованием Django и API OpenWeatherMap.

![Скриншот приложения] https://raw.githubusercontent.com/317zariba/WeatherApp/refs/heads/main/appearence.png

## 📁 Структура проекта

.
│ .env.example # Пример файла настроек окружения
│ db.sqlite3 # База данных SQLite
│ docker-compose.yml # Конфигурация Docker Compose
│ Dockerfile # Dockerfile для сборки образа
│ manage.py # Утилита управления Django
│ requirements.txt # Зависимости Python
│
├───weather # Основное приложение Django
│ │ admin.py # Настройки админ-панели
│ │ apps.py # Конфигурация приложения
│ │ forms.py # Формы для пользовательского ввода
│ │ models.py # Модели данных
│ │ tests.py # Тесты приложения
│ │ urls.py # Маршруты приложения
│ │ views.py # Логика представлений
│ │ init.py
│ │
│ ├───migrations # Миграции базы данных
│ │ init.py
│ │
│ └───templates # HTML шаблоны
│ └───weather
│ index.html # Главная страница
│
└───weather_project # Корневой пакет проекта
asgi.py # ASGI конфигурация
settings.py # Настройки Django
urls.py # Корневые маршруты
wsgi.py # WSGI конфигурация
init.py

## ✨ Особенности

- **Получение погоды в реальном времени** - Использует OpenWeatherMap API для получения актуальных данных о погоде
- **Простой и интуитивный интерфейс** - Чистый дизайн с адаптивной версткой
- **Кэширование запросов** - Оптимизация производительности за счет кэширования данных о погоде
- **Поддержка Docker** - Легкий запуск в контейнерах для разработки и продакшена
- **База данных SQLite** - Хранение истории поиска городов пользователей
- **Форма поиска** - Удобный поиск погоды по названию города

## 🚀 Запуск проекта

### 🐳 Запуск в Docker (рекомендуется)

Склонируйте репозиторий:
bash
git clone https://github.com/317zariba/WeatherApp.git
cd WeatherApp
Создайте файл .env на основе примера:
bash

cp .env.example .env
Заполните переменные окружения в файле .env:
env

API_KEY=ваш_api_key_openweathermap
SECRET_KEY=ваш_секретный_ключ_django
Запустите приложение с помощью Docker Compose:
bash

docker-compose up --build
Приложение будет доступно по адресу: http://localhost:8000
💻 Локальный запуск
Установите зависимости:
bash

pip install -r requirements.txt
Создайте и настройте файл .env:
bash

cp .env.example .env
Примените миграции базы данных:
bash

python manage.py migrate
Запустите сервер разработки:
bash

python manage.py runserver
Приложение будет доступно по адресу: http://localhost:8000
🔧 Настройка окружения
Создайте файл .env в корне проекта с следующими переменными:

env

# Ключ API от OpenWeatherMap (получите на https://openweathermap.org/api)
API_KEY=ваш_ключ_openweathermap

# Секретный ключ Django (сгенерируйте новый для продакшена)
SECRET_KEY=ваш_уникальный_секретный_ключ

# Дополнительные настройки (опционально)
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
🛠️ Требования
Python 3.8+
Docker и Docker Compose (для запуска в контейнерах)
API ключ от OpenWeatherMap

Автор: @317zariba
Репозиторий: https://github.com/317zariba/WeatherApp
Дата создания: 2025# WeatherApp

